# Device-local secret. Copy this to steamlink-ota.conf during provisioning.
# The value is deliberately not committed to the public repository.
STEAMLINK_OTA_PASSWORD=replace-with-a-random-device-secret
